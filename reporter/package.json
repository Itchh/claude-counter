{
  "name": "leaderboard-reporter",
  "version": "1.0.0",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "bun index.ts",
    "setup": "bun setup.ts",
    "remove": "bun uninstall.ts",
    "restart": "launchctl kickstart -k gui/$(id -u)/com.leaderboard.reporter",
    "logs": "tail -f $HOME/Library/Logs/leaderboard-reporter.log"
  },
  "dependencies": {
    "@clack/prompts": "^1.2.0",
    "chokidar": "^3.6.0",
    "picocolors": "^1.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
